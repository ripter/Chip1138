import expect from 'expect.js';
import Memory from '../src/memory.js';
import { random8bit } from './utils.js';

describe('Memory', () => {
  let memory;

  beforeEach(() => {
    memory = new Memory();
  })

});
